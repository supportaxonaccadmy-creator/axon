import { memo, useState, useEffect, useCallback } from 'react';
import { Ticket, Plus, Trash2, CheckCircle, XCircle } from 'lucide-react';
import { useCoupons } from '@/hooks/useCoupons';
import { couponService } from '@/services/finance';
import type { Coupon } from '@/services/finance';

function CouponManagerComponent() {
  const { coupons, loading, refresh, createCoupon, deleteCoupon } = useCoupons();
  const [showForm, setShowForm] = useState(false);
  const [form, setForm] = useState({ code: '', discountType: 'flat' as 'flat' | 'percentage', discountValue: 0, maxDiscount: 0, minOrderAmount: 0, maxUses: 100, expiresAt: '' });
  useEffect(() => { void refresh(); }, [refresh]);
  const handleCreate = useCallback(async () => {
    if (!form.code || form.discountValue <= 0) return;
    await createCoupon({ code: form.code, discountType: form.discountType, discountValue: form.discountValue, maxDiscount: form.maxDiscount > 0 ? form.maxDiscount : undefined, minOrderAmount: form.minOrderAmount, maxUses: form.maxUses > 0 ? form.maxUses : undefined, expiresAt: form.expiresAt || undefined } as Parameters<typeof couponService.createCoupon>[0]);
    setShowForm(false); setForm({ code: '', discountType: 'flat', discountValue: 0, maxDiscount: 0, minOrderAmount: 0, maxUses: 100, expiresAt: '' }); void refresh();
  }, [form, createCoupon, refresh]);
  const handleDelete = useCallback(async (id: string) => { await deleteCoupon(id); void refresh(); }, [deleteCoupon, refresh]);
  return (<div className="rounded-xl border border-neutral-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-center justify-between"><div className="flex items-center gap-2"><div className="flex h-9 w-9 items-center justify-center rounded-lg bg-primary-50"><Ticket className="h-4 w-4 text-primary-600" /></div><h3 className="text-sm font-semibold text-neutral-900">Coupon Manager</h3></div><button onClick={() => setShowForm(!showForm)} className="flex items-center gap-1 rounded-lg bg-primary-600 px-3 py-1.5 text-xs font-medium text-white hover:bg-primary-700"><Plus className="h-3.5 w-3.5" /> New Coupon</button></div>{showForm && (<div className="mb-4 space-y-3 rounded-lg border border-neutral-200 p-4"><div className="grid grid-cols-2 gap-3"><div><label className="text-xs text-neutral-500">Code</label><input value={form.code} onChange={(e) => setForm({ ...form, code: e.target.value.toUpperCase() })} className="mt-1 w-full rounded-lg border border-neutral-300 px-3 py-1.5 text-sm" placeholder="SAVE20" /></div><div><label className="text-xs text-neutral-500">Type</label><select value={form.discountType} onChange={(e) => setForm({ ...form, discountType: e.target.value as 'flat' | 'percentage' })} className="mt-1 w-full rounded-lg border border-neutral-300 px-3 py-1.5 text-sm"><option value="flat">Flat (₹)</option><option value="percentage">Percentage (%)</option></select></div></div><div className="grid grid-cols-3 gap-3"><div><label className="text-xs text-neutral-500">Value</label><input type="number" value={form.discountValue} onChange={(e) => setForm({ ...form, discountValue: Number(e.target.value) })} className="mt-1 w-full rounded-lg border border-neutral-300 px-3 py-1.5 text-sm" /></div><div><label className="text-xs text-neutral-500">Max Discount</label><input type="number" value={form.maxDiscount} onChange={(e) => setForm({ ...form, maxDiscount: Number(e.target.value) })} className="mt-1 w-full rounded-lg border border-neutral-300 px-3 py-1.5 text-sm" /></div><div><label className="text-xs text-neutral-500">Max Uses</label><input type="number" value={form.maxUses} onChange={(e) => setForm({ ...form, maxUses: Number(e.target.value) })} className="mt-1 w-full rounded-lg border border-neutral-300 px-3 py-1.5 text-sm" /></div></div><div><label className="text-xs text-neutral-500">Expiry Date</label><input type="date" value={form.expiresAt} onChange={(e) => setForm({ ...form, expiresAt: e.target.value })} className="mt-1 w-full rounded-lg border border-neutral-300 px-3 py-1.5 text-sm" /></div><button onClick={handleCreate} className="rounded-lg bg-primary-600 px-4 py-2 text-sm font-medium text-white hover:bg-primary-700">Create Coupon</button></div>)}<div className="space-y-2">{coupons.map((coupon: Coupon) => (<div key={coupon.id} className="flex items-center justify-between rounded-lg border border-neutral-100 p-3 text-xs"><div className="flex items-center gap-3">{coupon.isActive ? <CheckCircle className="h-4 w-4 text-success-500" /> : <XCircle className="h-4 w-4 text-error-500" />}<div><p className="font-mono font-semibold text-neutral-900">{coupon.code}</p><p className="text-neutral-400">{coupon.discountType === 'flat' ? `₹${coupon.discountValue} off` : `${coupon.discountValue}% off`} | Used: {coupon.usedCount}/{coupon.maxUses ?? '∞'}</p></div></div><button onClick={() => handleDelete(coupon.id)} className="rounded p-1 text-error-500 hover:bg-error-50"><Trash2 className="h-3.5 w-3.5" /></button></div>))}{coupons.length === 0 && !loading && <p className="text-sm text-neutral-400">No coupons yet. Create one to get started.</p>}</div></div>);
}
export const CouponManager = memo(CouponManagerComponent);
